# Optional: Gemini API key if you integrate Google AI from a secure backend or tooling.
# Do not expose production keys in the browser; prefer a server-side proxy.
GEMINI_API_KEY=""

# Optional: canonical URL for your deployed app (links, callbacks, etc.).
APP_URL=""
